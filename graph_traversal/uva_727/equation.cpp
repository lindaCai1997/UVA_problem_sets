#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <vector>
#include <locale>
#include <algorithm>
#include <fstream>
#include <map>
#include <ctype.h>
#include <cmath>
#include <typeinfo>
#include <limits.h>
#include <map>
#include <time.h>
#include <string.h>
#include <stack>
#include <queue>

using namespace std;

struct node{
	char op; 
	node* left;
	node* right;
};

int main(){
	char line[100] = {0};
	char c = 0;
	int size = 0;
	while (scanf("%c", %c) > 0){
		line[size++] = c;
	}	
	
	for (int i = 0; i < size; i++){
		if (line[i] == '('){
			//find according )
			//build the branch tree
			//use it as left branch, cache the operator after ()
		}
		else if (line[i] >=0 && line[i] <= 9){


		}
				
	


	}
	return 0;
}
	
